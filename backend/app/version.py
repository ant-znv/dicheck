"""Версия приложения. Единственный источник истины для UI, установщика и апдейтера.

Процедура релиза: поднять APP_VERSION → закоммитить → тег vX.Y.Z → push.
CI соберёт установщик с /DMyAppVersion=X.Y.Z и опубликует GitHub Release.
"""

APP_VERSION = "0.2.1"
